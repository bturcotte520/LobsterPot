{
  "name": "lobsterpot",
  "version": "0.2.0",
  "private": true,
  "description": "iMessage-style iOS app for OpenClaw Gateway sessions",
  "scripts": {
    "ios:open": "cd apps/ios && xcodegen generate && open LobsterPot.xcodeproj"
  }
}
